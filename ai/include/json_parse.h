#ifndef JSON_PARSE_H
#define JSON_PARSE_H
#include <string>
#include <vector>
#include "lines.h"

class JSON_parse
{
    public:
        JSON_parse();
        JSON_parse( string arg );
        move_list data;

    protected:

    private:
};

#endif // JSON_PARSE_H
